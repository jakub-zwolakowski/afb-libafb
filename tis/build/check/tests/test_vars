# defined to 1 if subunit is enabled
ENABLE_SUBUNIT=0
export ENABLE_SUBUNIT
EXEEXT=
export EXEEXT
HAVE_FORK=1
export HAVE_FORK

# path of the tests directory
if [ x"/home/qba/git/afb-libafb/check/tests" != x"." ]; then
    if [ -z "" -o "" != "1" ]; then
       SRCDIR="/home/qba/git/afb-libafb/check/tests/"
    else
       SRCDIR="/home/qba/git/afb-libafb/check/tests\\"
    fi
else
    SRCDIR=""
fi

export SRCDIR
